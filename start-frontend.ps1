# Start Frontend Server
Write-Host "🚀 Starting Frontend Server (React + Vite)..." -ForegroundColor Cyan

Set-Location "d:\backend1\frontend"

Write-Host "Frontend server đang chạy tại: http://localhost:3000" -ForegroundColor Green
npm run dev
